"""构建工具函数（预留空实现）

当前为空实现，用于演示预留架构。

当多个构建类技能需要共享工具函数时，可在此实现：
- scan_artifacts(build_dir) -> list[Artifact]
- detect_toolchain(arch) -> ToolInfo
- parse_cmake_output(output) -> dict
- filter_artifacts(artifacts, min_size) -> list[Artifact]
"""

# 空实现占位
